name: Github Actions Demo
on: [push]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automaticallt triggered by a ${{github.event_name}} event."
      - run: python main.py